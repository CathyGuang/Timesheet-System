/* CREATE USER */
-- User: "ForwardStrideAdmin"
-- TO RESET:
-- DROP USER "ForwardStrideAdmin";

CREATE USER "ForwardStrideAdmin" WITH
  LOGIN
  SUPERUSER
  INHERIT
  CREATEDB
  CREATEROLE
  NOREPLICATION;

/* CREATE DATABASE */
-- Database: ForwardStrideDatabase
-- TO RESET:
-- DROP DATABASE "ForwardStrideDatabase";

CREATE DATABASE "ForwardStrideDatabase"
    WITH
    OWNER = "ForwardStrideAdmin"
    ENCODING = 'UTF8'
    LC_COLLATE = 'English_United States.1252'
    LC_CTYPE = 'English_United States.1252'
    TABLESPACE = pg_default
    CONNECTION LIMIT = -1;

/* SWITCH USER AND CONNECT TO NEW DATABASE??? */



/* CREATE CLASS TABLE */

CREATE TYPE CLASS_TYPE AS ENUM();
CREATE TYPE ARENA AS ENUM();
CREATE TYPE HORSE_BEHAVIOR AS ENUM();
CREATE TYPE TACK AS ENUM();
CREATE TYPE PAD AS ENUM();

ALTER TYPE ARENA ADD VALUE 'none';
ALTER TYPE HORSE_BEHAVIOR ADD VALUE 'none';
ALTER TYPE TACK ADD VALUE 'none';
ALTER TYPE PAD ADD VALUE 'none';

CREATE TABLE classes (
  id SERIAL PRIMARY KEY,
  class_type CLASS_TYPE,
  date_of_class DATE,
  start_time TIME,
  end_time TIME,
  arena ARENA,
  lesson_plan TEXT,
  horse INT REFERENCES horses(id),
  horse_behavior HORSE_BEHAVIOR,
  tack TACK,
  special_tack TEXT,
  stirrup_leather_length TEXT,
  pad PAD,
  clients INT [],
  attendance INT [],
  client_notes TEXT,
  instructor INT REFERENCES workers(id),
  therapist INT REFERENCES workers(id),
  equine_specialist INT REFERENCES workers(id),
  leader INT REFERENCES workers(id),
  sidewalkers INT []
);


/* CREATE WORKERS TABLE */

CREATE TABLE workers (
  id SERIAL PRIMARY KEY,
  name TEXT,
  email TEXT,
  phone TEXT,
  staff BOOLEAN,
  volunteer BOOLEAN
);


/* CREATE CLIENTS TABLE */

CREATE TABLE clients (
  id SERIAL PRIMARY KEY,
  name TEXT,
  email TEXT,
  phone TEXT
);


/* CREATE HORSES TABLE */

CREATE TABLE horses (
  id SERIAL PRIMARY KEY,
  name TEXT,
  fs_uses_per_day INT,
  owner_uses_per_day INT,
  notes TEXT
);


/* CREATE HORSE CARE TABLE */

CREATE TYPE CARE_TYPE AS ENUM();
ALTER TYPE CARE_TYPE ADD VALUE 'none';

CREATE TABLE horse_care (
  id SERIAL PRIMARY KEY,
  care_type CARE_TYPE,
  date_of_shift DATE,
  time_of_shift TIME,
  volunteers INT []
);


/* CREATE OFFICE SHIFT TABLE */

CREATE TYPE OFFICE_SHIFT_TYPE AS ENUM();
ALTER TYPE OFFICE_SHIFT_TYPE ADD VALUE 'none';

CREATE TABLE office_shifts (
  id SERIAL PRIMARY KEY,
  office_shift_type OFFICE_SHIFT_TYPE,
  date_of_shift DATE,
  time_of_shift TIME,
  volunteers INT []
);


/* CREATE VOLUNTEER HOURS TABLE */


CREATE TABLE volunteer_hours (
  id SERIAL PRIMARY KEY,
  volunteer INT REFERENCES workers(id),
  hours INT,
  shift_type TEXT
);


/* INITIALIZE NECESSARY NULL VALUES */
INSERT INTO workers (name) VALUES ('none');
INSERT INTO clients (name) VALUES ('none');
INSERT INTO horses (name) VALUES ('none');
