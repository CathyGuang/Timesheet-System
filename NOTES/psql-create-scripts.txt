/* CREATE DATABASE */


/* CREATE CLASS TABLE */


/* CREATE WORKERS TABLE */

CREATE TABLE workers (
  id SERIAL PRIMARY KEY,
  name TEXT,
  email TEXT,
  phone TEXT,
  staff BOOLEAN,
  volunteer BOOLEAN
);


/* CREATE CLIENTS TABLE */

CREATE TABLE clients (
  id SERIAL PRIMARY KEY,
  name TEXT,
  email TEXT,
  phone TEXT
);


/* CREATE HORSES TABLE */

CREATE TABLE horses (
  id SERIAL PRIMARY KEY,
  name TEXT,
  fs_uses_per_day INT,
  owner_uses_per_day INT,
  notes TEXT
);


/* CREATE HORSE CARE TABLE */

CREATE TABLE horse_care (
  id SERIAL PRIMARY KEY,
  /*care_type ENUM*//*,*/
  date_of_shift DATE,
  time_of_shift TIME,
  volunteers INT []
);


/* CREATE OFFICE SHIFT TABLE */

CREATE TABLE office_shifts (
  id SERIAL PRIMARY KEY,
  /*office_shift_type ENUM*//*,*/
  date_of_shift DATE,
  time_of_shift TIME,
  volunteers INT []
);


/* CREATE VOLUNTEER HOURS TABLE */

CREATE TABLE volunteer_hours (
  id SERIAL PRIMARY KEY,
  volunteer INT REFERENCES volunteer(id),
  hours INT/*,*/
  /* shift_type ENUM CUSTOM TYPE */
);
